apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ziti-host-master.fullname" . }}-test-connection"
  labels:
    {{- include "ziti-host-master.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ziti-host-master.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
